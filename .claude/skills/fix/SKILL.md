---
description: Исправляет конкретную проблему в проекте. Показывает план изменений простым языком и ждёт подтверждения. Использование: /fix <описание проблемы или номер из аудита>
allowed-tools: Read, Grep, Glob, Write, Edit, Bash(cd:*), Bash(python:*), Bash(git:*), Bash(cat:*)
---

# Исправление проблемы в Pravda Market

ultrathink

## Аргумент: $ARGUMENTS

## Порядок действий

### Шаг 1: Анализ
- Найди файлы, связанные с описанной проблемой
- Прочитай их и пойми контекст
- Определи все места, которые нужно изменить

### Шаг 2: План (ОБЯЗАТЕЛЬНО показать пользователю)
Перед любыми изменениями, покажи план в таком формате:

```
📋 ПЛАН ИСПРАВЛЕНИЯ

Проблема: [описание простым языком]
Почему это важно: [что пойдёт не так если не починить]

Что буду менять:
1. [файл] — [что именно меняю и зачем]
2. [файл] — [что именно меняю и зачем]
...

Риски: [может ли что-то сломаться от этих изменений]

Подтвердите? (да/нет)
```

### Шаг 3: Исправление
- Вноси изменения ТОЛЬКО после подтверждения пользователя
- Каждое изменение комментируй: что было → что стало
- Если изменение затрагивает тесты — обнови и тесты

### Шаг 4: Проверка
- После изменений проверь что ничего не сломалось
- Запусти линт или тесты для изменённых файлов
- Покажи diff изменений

## Правила
- НИКОГДА не меняй код без показа плана
- НИКОГДА не удаляй код без объяснения
- Если не уверен — спроси
- Объясняй ВСЁ простым языком, как будто пользователь не программист
- Если исправление может сломать другие части — предупреди
