---
description: Полная проверка здоровья проекта. Находит ошибки, проблемы безопасности, битые импорты и объясняет всё простым языком.
allowed-tools: Read, Grep, Glob, Bash(cd:*), Bash(python:*), Bash(cat:*), Bash(find:*)
---

# Проверка здоровья проекта Pravda Market

ultrathink

## Что нужно проверить

### 1. Безопасность
- Есть ли хардкод-секреты в коде (токены, пароли, ключи)
- Правильно ли настроен `.gitignore` для `.env*` файлов
- CORS настройки — нет ли `"*"` в продакшен конфигурации
- Fallback-значения для секретных переменных (должны быть `None`, не строка)
- Docker — работает ли от root

### 2. Код backend
- Проверить `backend/app/` на битые импорты
- Проверить что все модели SQLAlchemy используют правильные типы (BigInteger для telegram_id, etc.)
- Проверить что datetime всегда timezone-aware
- Проверить что ошибки БД не утекают в HTTP responses
- Проверить что `assert` не используется для валидации

### 3. Код frontend
- Проверить `frontend/src/` на TypeScript ошибки
- Проверить мёртвый код (неиспользуемые компоненты, функции, типы)
- Проверить что `parseInt` имеет NaN guard
- Проверить `vite.config.ts` — правильный ли `base` path

### 4. Тесты
- Запустить `cd backend && python -m pytest --tb=short 2>&1 | tail -20`
- Запустить `cd frontend && npx vitest run 2>&1 | tail -20`
- Проверить что моки соответствуют реальным типам

### 5. Инфраструктура
- Совпадает ли версия Python в Docker и CI
- Зависит ли deploy от тестов
- Запускаются ли миграции автоматически

## Формат ответа

Отвечай ПРОСТЫМ ЯЗЫКОМ. Для каждой проблемы:

**🔴 КРИТИЧНО** — проект сломается или есть угроза безопасности
**🟡 ВАЖНО** — может сломаться в проде, стоит починить
**🟢 МЕЛОЧЬ** — не срочно, но стоит сделать

Для каждой проблемы объясни:
1. **Что случилось** — в одном предложении
2. **Почему это плохо** — что пойдёт не так
3. **Как починить** — конкретные шаги

В конце дай общую оценку: готов ли проект к продакшену.
