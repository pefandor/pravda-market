---
description: Глубокий аудит безопасности проекта. Проверяет секреты, уязвимости, CORS, auth, SQL injection, XSS и другие угрозы. Особое внимание на финансовые операции (prediction market).
allowed-tools: Read, Grep, Glob, Bash(cd:*), Bash(grep:*), Bash(find:*), Bash(cat:*)
---

# Аудит безопасности Pravda Market

ultrathink

## Контекст
Это prediction market (аналог Polymarket) — приложение работает с деньгами пользователей.
Любая уязвимость в безопасности может привести к финансовым потерям.

## Чеклист проверки

### 🔑 Секреты и токены
- Grep по всему проекту на хардкод-токены, пароли, ключи API
- Проверить все `.env` файлы — нет ли реальных токенов в репозитории
- Проверить fallback-значения в `os.getenv()` — не должно быть рабочих дефолтов
- Проверить `.gitignore` — все `.env*` файлы должны быть исключены
- Проверить git history на случайно закоммиченные секреты: `git log --all --diff-filter=A -- '*.env*'`

### 🔐 Аутентификация
- Проверить HMAC-SHA256 валидацию в `security.py`
- Проверить что auth нельзя обойти (bypass)
- Проверить expiry проверки — timezone-aware?
- Проверить admin endpoints — нужен ли дополнительный auth

### 💰 Финансовые операции (КРИТИЧНО для prediction market)
- Проверить что нельзя разместить ставку с отрицательной суммой
- Проверить что нельзя получить больше денег чем положено при резолюции
- Проверить что баланс не может стать отрицательным
- Проверить race conditions при одновременных ставках
- Проверить что settlement (выплата) атомарна — нет partial state
- Проверить что нельзя отменить уже зарезолвленный маркет

### 🌐 Веб-безопасность
- CORS настройки — не `"*"` в проде
- Rate limiting — есть ли на тяжёлых endpoints
- SQL injection — используется ли ORM правильно, нет ли raw SQL
- XSS — санитизация пользовательского ввода
- Error messages — не утекают ли детали БД/стека

### 🐳 Инфраструктура
- Docker — не от root
- Зависимости — есть ли известные уязвимости
- HTTPS — настроен ли в проде

## Формат ответа

Для каждой найденной уязвимости:

```
🔴 [CRITICAL/HIGH/MEDIUM/LOW] — Название

Где: файл:строка
Что: описание проблемы простым языком
Угроза: что может сделать злоумышленник
Как починить: конкретные шаги
```

В конце:
- Общая оценка безопасности (1-10)
- Топ-3 что нужно исправить НЕМЕДЛЕННО
- Готов ли проект к работе с реальными деньгами
